{
  "version": 2,
  "builds": [
    {
      "src": "Dockerfile",
      "use": "@vercel/docker"
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
  ],
  "env": {
    "WEASYPRINT_FONTS_PATH": "/usr/share/fonts"
  }
}
